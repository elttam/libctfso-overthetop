#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
#include <fcntl.h>

int main(int argc, char **argv)
{
	char *a = malloc(64);
	char *b = malloc(64);
	strcpy(a, argv[1]);
	printf("%s\n", a);
	free(a);
	return 0;
}
